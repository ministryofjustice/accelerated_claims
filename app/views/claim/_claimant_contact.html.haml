= claim_form.fields_for claimant_field, claimant_contact, builder: LabellingFormBuilder do |form|
  .sub-panel.claimant-solicitor
    .sub-panel.details
      .row.js-only
        %a.caption#contact-details{ href: '#contact-details' }
          = 'Add phone and email'.html_safe
      = form.text_field_row :email, class: 'rel email'
      = form.text_field_row :phone, class: 'rel phone'
      = form.text_field_row :fax, class: 'rel fax'
      = form.text_field_row :dx_number, label: 'DX number', class: 'rel dx_number'
    .row
      You can add another address to be used for correspondance about the case.
      %span.hint.block
        Optional. eg a legal representative's address
    .sub-panel.details
      .row.js-only
        %a.caption#correspondence-address{ href: '#correspondence-address' }
          = 'Add alternative address'.html_safe
      = form.text_field_row :title,    class: 'rel title', input_class: 'smalltext'
      = form.text_field_row :full_name, class: 'rel '
      = form.text_field_row :company_name,  class: 'rel company_name'
      = form.text_area_row  :street,   label: 'Street address', class: 'rel street'
      = form.text_field_row :postcode, class: 'rel postcode', input_class: 'smalltext'
